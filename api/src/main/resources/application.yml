spring:
  name: api
  datasource:
    postgresql:
      url: jdbc:postgresql://makila-postgres.cymtbjk7fwn8.us-east-1.rds.amazonaws.com:5432/Delistore 
      username: makila
      password: pg2mongo
      driver-class-name: org.postgresql.Driver
  
  data:
    mongodb:
      uri: mongodb+srv://makila:pg2mongo@makila-demo.drtak.mongodb.net/?retryWrites=true&w=majority&appName=makila-demo
      database: makila3
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.springframework.data.mongodb: DEBUG
    org.springframework.data.postgresql: DEBUG
    